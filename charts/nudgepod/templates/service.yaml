apiVersion: v1
kind: Service
metadata:
  name: {{ include "nudgepod.fullname" . }}
  labels:
    {{- include "nudgepod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nudgepod.selectorLabels" . | nindent 4 }}
