apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nudgepod.fullname" . }}-test-connection"
  labels:
    {{- include "nudgepod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nudgepod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
